import type { Meta, StoryObj } from '@storybook/web-components-vite';
import './textarea.ts';
const meta: Meta = {
  component: 'deform-textarea',
};

export default meta;
type Story = StoryObj;

export const Primary: Story = {
  args: {
  },
};